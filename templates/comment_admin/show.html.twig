{% extends 'base.html.twig' %}

{% block title %}Comment{% endblock %}

{% block body %}
    <h1>Comment</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ comment.id }}</td>
            </tr>
            <tr>
                <th>Comment</th>
                <td>{{ comment.comment }}</td>
            </tr>
            <tr>
                <th>CommenterName</th>
                <td>{{ comment.commenterName }}</td>
            </tr>
            <tr>
                <th>PublishedAt</th>
                <td>{{ comment.publishedAt ? comment.publishedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_comment_admin_index') }}">back to list</a>

    <a href="{{ path('app_comment_admin_edit', {'id': comment.id}) }}">edit</a>

    {{ include('comment_admin/_delete_form.html.twig') }}
{% endblock %}
